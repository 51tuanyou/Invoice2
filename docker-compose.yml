version: '3.8'

services:
  invoice-ocr-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    container_name: invoice-ocr-frontend
